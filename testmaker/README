<백엔드> 1. nodejs로 서버 만들기 - localhost:3025 서버 만들기

    <프론트엔드>
        1. 프론트 설정하기
         - react,react-dom,typescript,@types/react, @types/react-dom
            : react를 사용할 것이기 떄문에 react와 react-dom을 깔아 react가 제공하는 함수를 사용할 것이고 가독성이 떨어지지만 더 안전한 typescript를 기반으로 하기 위해 typescrip를 설치한다.
        - eslint, prettier
            : eslint는 javascript문법의 오류를 안내해주고, prettier는 코드를 자동으로 코드 포멧을 해준다. 코드포멧을 어떻게 할지 설정할 수 있으며 이것이 .prettierrc에 설정되어있다.
        -tsconfig.json 설정
            : typescript를 js로 변환할 때 어떻게 뱐환할지에 설정하는 파일이다.
        -webpack.config.ts 설정
            :파일들을 어떻게 js로 변환할 것인지 설정들을 해놓은 파일이다.
        -@babel/preset-env
            :바벨이 최신 자바스크립트 문법을 사용해도 설정해놓은 브라우저에서 다 돌아가도록 문법을 바꿔주는 라이브러리이다.
        -@babel/preset-typescript
            :타입스크립트코드들을 javascrip로 변환해준다.
        -@babel/preset-react
            :리액트 코드들을(jsx)들을 javascript로 변환해준다.

        -ts-node
            :웹팩이 ts코드를 해석하지 못한다. tsconfig.json에 설정을 해줘야하는데 ts-node를 사용한다. 웹팩이 ts-node가 있어야 이를 해석할 수 있기에 ts-node를 설치해준다.

        -webpack-dev-server를 만든다.
            : server가 없으면 매번 프론트를 변경할 때마다 build를 해야한다. server가 있으면 변경되었을 때 자동으로 변경을 반영해준다.

        -fork-ts-checker-webpack-plugin 설정
            : webpack은 ts를 체크하고 eslint를 실행하는데, 이 플러그인은 동시에 체크해주어서 더 효율적이다.

    2. 회원가입, 로그인페이지만들기
        - css는 emotion으로 조작한다. main페이지 header를 만든다.
        -
